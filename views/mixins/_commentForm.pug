mixin commentForm(repair)
  form.form-group(action=`/comment/${repair._id}` method="POST")
    textarea.form-control(name="text" placeholder="Seu comentário aqui")
    input.btn.btn-secondary.form-control.my-2(type="submit" value="Adicione seu comentário")
