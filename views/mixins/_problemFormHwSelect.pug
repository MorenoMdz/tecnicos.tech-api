
mixin problemFormHwSelect(problem={}) 
  form(action=`/addNewProblem` method="POST"  class="card" enctype="multipart/form-data" id="problemForm")

    label(for="hardware") Escolha qual aparelho
    select(name='hardware' form="problemForm")
      option(value='' selected=!hardwares) (Selecione um console)
      for hardware in hardwares
        option(value=hardware._id selected=(hardware._id === problem.hardware)) #[strong #{hardware.name}] || Modelo: #{hardware.model}

    label(for="title") Título do Defeito
    input(type="text" name="title" placeholder="Digite um título para o defeito" value=problem.title)

    label(for="description") Descrição detalhada do problema
    textarea(name="description" placeholder="descrição")= problem.description

    label(for="board_model") Modelo da placa: 
    input(type="text" name="board_model" placeholder="Insira o modelo da placa" value=problem.board_model)

    label(for="photo") Photo
      input(type="file" name="photo" id="photo" accept="image/gif, image/png, image/jpeg")
      if problem.photo
        img(src=`/uploads/${problem.photo}`, alt=problem.name width=200)

    label(for="video") Link para um video
    span Ideal usar youtube / streamable / vimeo
    input(type="text" name="video" placeholder="link para o video" value=problem.video)

    //- label(for="video") Video Placeholder
      input(type="file" name="video" id="video" accept="image/gif, image/png, image/jpeg")
      if problem.video
        img(src=`/uploads/${problem.video}`, alt=problem.name width=200)

    hr

  input(type="submit" value="Salvar" class="button")



    