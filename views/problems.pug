extends layout

include mixins/_problemsList.pug

block content
  //pre= h.dump(problems)
  .inner  
    p: strong Defeitos do aparelho: 
    p orderar por: #[strong TODO] 
      #hwListP(style="display:block")
        +problemsList(problems)
        
