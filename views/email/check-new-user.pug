extends email-layout

block content
  h2 Olá Boss!
  p Novo usuário registrado, verifique os dados e ative ou rejeite a requisição:
  +button(adminUrl, ' Confirme o novo usuário →')
  p Se você não conseguir clicar no link acima acesse pelo #[a(href=`http://${adminUrl}`) site]
  br
  hr
